{
  "name": "ayudaparalamujer",
  "description": "blog de ayuda para la mujer en situaciones de violencia de genero y maltrato domestico en puerto vallarta, jalisco, mexico",
  "scripts": {
    "tailwind": "npx tailwindcss -i ./src/tailwind.css -o ./_site/styles.css --watch",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && npx tailwindcss -i ./src/tailwind.css -o ./_site/styles.css --minify"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@tailwindcss/typography": "^0.5.10",
    "daisyui": "^3.9.4",
    "tailwindcss": "^3.4.1"
  }
}